# Copie para .env e preencha. NÃO comite o .env (já está no .gitignore).

# Token do Multi360 — pegue no navegador logado:
#   F12 → Application → Local Storage → painel.multi360.com.br → ngStorage-token
#   Copie o valor SEM as aspas externas.
MULTI360_TOKEN=

# Supabase do hub 3W
SUPABASE_URL=https://zaitvvwoqwdgtliocvtf.supabase.co
# service_role key: Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=
